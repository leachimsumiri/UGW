# INFO: Already prepared for later use of SQL Alchemy, for now directly use supabase

# from flask_sqlalchemy import SQLAlchemy
# db = SQLAlchemy()

# def init_app(app):
#     db.init_app(app)
#     db.create_all(app=app)